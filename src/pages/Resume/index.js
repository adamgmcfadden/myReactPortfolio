import React from "react";

const Resume = () => (
  <div>
    <h1 className="title">Resume Page</h1>
    <p>Temporary Resume Page - Maybe add PDF link to actual resume instead?</p>
  </div>
);

export default Resume;
